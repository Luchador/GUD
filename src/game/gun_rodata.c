#include <ultra64.h>

/*
 * Keep these values as raw words so the compiler emits their exact bit patterns and
 * so the linker can retain the original order of gun.c's literal pool.
 */
const u32 g_GLGrenadeLaunchUnk8C[1] = { 0x3e99999a };
const u32 g_GLGrenadeLaunchUnk94[1] = { 0x3e088888 };
const u32 g_TankShellSpeed[1] = { 0x42855555 };
const u32 D_80053DDC[1] = { 0x3f8e38e3 };
